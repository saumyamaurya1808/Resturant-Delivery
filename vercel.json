{
  "version": 2,
  "builds": [
    {
      "src": "frontEnd/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "^/(.*)$", "dest": "/frontEnd/$1" }
  ]
}
